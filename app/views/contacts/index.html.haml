%h1 Listing contacts

%table
  %thead
    %tr
      %th Name
      %th Date of birth
      %th Telephone
      %th Address
      %th Credit card number
      %th Email
      %th Import status
      %th User
      %th Credit card network
      %th
      %th
      %th

  %tbody
    - @contacts.each do |contact|
      %tr
        %td= contact.name
        %td= contact.date_of_birth
        %td= contact.telephone
        %td= contact.address
        %td= contact.credit_card_number
        %td= contact.email
        %td= contact.import_status
        %td= contact.user
        %td= contact.credit_card_network
        %td= link_to 'Show', contact
        %td= link_to 'Edit', edit_contact_path(contact)
        %td= link_to 'Destroy', contact, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Contact', new_contact_path
